package {		import flash.display.MovieClip;	import flash.events.KeyboardEvent;	import flash.events.TimerEvent;	import flash.utils.Timer;	public class Retro extends MovieClip {		var rgb, map;			public function Retro() {			map = new MapGenerator();			addChild(map);			stage.addEventListener(KeyboardEvent.KEY_DOWN, startRGB); 			// addChildAt(rgb, 1); 			// var RGBtimer:Timer = new Timer(1000);			// RGBtimer.addEventListener(TimerEvent.TIMER, updateRetro);			// RGBtimer.start();			// var noise = new Noise(stage.stageWidth, stage.stageHeight);			// addChild(noise);			// var scanlines = new Scanlines(stage.stageWidth, stage.stageHeight);			// addChild(scanlines);			// var rollBar = new RollBar(stage.stageWidth, stage.stageHeight);			// addChild(rollBar);				}		function startRGB(keyEvent:KeyboardEvent) {			if(!rgb) {				rgb = new RGBShift(map); 									removeChild(map);				addChildAt(rgb, 0); 								}		} 		function updateRetro(e:TimerEvent):void{			// titleScreen.mainText.text = e.currentTarget.currentCount;			// removeChild(rgb);						// rgb = new RGBShift(titleScreen); 			// addChildAt(rgb, 1); 		}	}}